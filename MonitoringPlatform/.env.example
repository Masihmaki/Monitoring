# Copy this file to .env in the same folder (never commit .env).
# Local Docker Postgres — change the password before any shared or public deploy.

POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me
POSTGRES_DB=monitoring_db
POSTGRES_PORT=5432
